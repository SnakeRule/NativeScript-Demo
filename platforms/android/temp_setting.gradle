android {
            defaultConfig {
                applicationId = "org.nativescript.NSdemo"

                if (false) {
                    minSdkVersion = false
                }

                if (false) {
                    targetSdkVersion = false
                }
            }
        }